; Railway/Nixpacks corre el install con NODE_ENV=production, que por defecto
; omite devDependencies. El build (next build) necesita @tailwindcss/postcss,
; tailwindcss, typescript y @types/*, todos en devDependencies. Forzar install:
include=dev
